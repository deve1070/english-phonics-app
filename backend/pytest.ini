[pytest]
testpaths = tests
# Import mode matters here: the repo has an `alembic/` directory that
# shadows the installed alembic package on the default sys.path, so tests
# must run with the project root on the path but the cwd handled by
# rootdir rather than by a bare "" entry.
pythonpath = .
asyncio_mode = auto
