include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # build_runner output. Rewriting these by hand — `dart fix` will offer
    # to — lasts until the next generation run and no longer.
    - "**/*.config.dart"
    - "**/*.g.dart"
    - "**/*.freezed.dart"
